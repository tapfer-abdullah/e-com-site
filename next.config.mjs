/** @type {import('next').NextConfig} */
// const nextConfig = {};

// export default nextConfig;


// next.config.mjs
const nextConfig = {
    images: {
        // domains: ['i.ibb.co', 'example.com', 'another-domain.com'],
        domains: ['*', 'i.ibb.co'],
    },
};

export default nextConfig;
